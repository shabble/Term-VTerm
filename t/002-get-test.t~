#!/usr/bin/env perl

use strict;
use warnings;

use Test::More tests => 3;
use Data::Dumper;

BEGIN {
    use_ok 'Term::VTerm';
}

my $vt = new_ok 'Term::VTerm->new'. [];

my $str = $vt->test;

is($str, 'Hello, World!', 'test returns correct value');

done_testing;

